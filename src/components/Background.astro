---
import "@/styles/global.css";
import "tailwindcss";
import { dataHeaderContent } from "@/data/home";
---

{dataHeaderContent.map((home)=>(
    <img src={home.backgroundHero} alt="Bacground" class="relative w-full h-[1024px] object-cover scale-100 -z-1"/>
))}